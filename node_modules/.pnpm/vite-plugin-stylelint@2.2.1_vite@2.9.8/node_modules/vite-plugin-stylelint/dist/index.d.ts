import Vite from 'vite';
import Stylelint from 'stylelint';
import { FilterPattern } from '@rollup/pluginutils';

interface Options extends Stylelint.LinterOptions {
    cache?: boolean;
    cacheLocation?: string;
    include?: FilterPattern;
    exclude?: FilterPattern;
    stylelintPath?: string;
    throwOnError?: boolean;
    throwOnWarning?: boolean;
    emitError?: boolean;
    emitErrorAsWarning?: boolean;
    emitWarning?: boolean;
    emitWarningAsError?: boolean;
}
declare function StylelintPlugin(options?: Options): Vite.Plugin;

export { Options, StylelintPlugin as default };
